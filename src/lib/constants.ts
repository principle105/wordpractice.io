import type { TextCategory } from "./types";

export const textCategories: { [key: string]: TextCategory } = {
    "Dictionary Easy": "dictionary easy",
    "Dictionary Hard": "dictionary hard",
    "Quote Easy": "quote easy",
    "Quote Hard": "quote hard",
};
